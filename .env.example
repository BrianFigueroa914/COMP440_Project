# Copy this file to .env and update the values for your local MySQL setup.
# This file is ignored by Git, so each developer can keep their own settings.

DB_URL=jdbc:mysql://127.0.0.1:3306/mydb?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
DB_USER=root
DB_PASSWORD=password
